- Stochastic time series generation for PV power and wind speed using first order Markov Chains.

- The code generates by default timeseries with hourly interval values (8760 values each scenario in a CSV file).

- Using hourly_timestamps_selector file, the CSV of each scenario can be modified with the desired time interval (repeating the hourly value for the whole hour).